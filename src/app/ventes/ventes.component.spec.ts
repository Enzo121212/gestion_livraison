import { VentesComponent } from './ventes.component';

describe('VentesComponent', () => {
  let component: VentesComponent;

  beforeEach(() => {
    component = new VentesComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
